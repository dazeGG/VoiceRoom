{
  "name": "voice-room",
  "version": "1.7.0",
  "private": true,
  "description": "Simple link-based LiveKit voice chat with screen sharing.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "npm --workspace @voice-room/api run start",
    "dev": "npm --workspace @voice-room/api run dev",
    "dev:api": "npm --workspace @voice-room/api run dev",
    "dev:web": "npm --workspace @voice-room/web run dev",
    "dev:livekit": "docker run --rm -p 7880:7880 -p 7881:7881 -p 7882:7882/udp livekit/livekit-server:latest --dev --bind 0.0.0.0",
    "build": "npm --workspace @voice-room/web run build",
    "check": "npm --workspace @voice-room/shared run check && npm --workspace @voice-room/api run check && npm --workspace @voice-room/web run check",
    "test": "npm --workspace @voice-room/shared run test && npm --workspace @voice-room/api run test"
  },
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": "20.20.2",
    "npm": "10.8.2"
  },
  "license": "MIT"
}
