import {expect,test} from '@playwright/test';import {readFileSync} from 'node:fs';
const inbox=readFileSync(new URL('../src/lib/shared/notifications/inbox.svelte.ts',import.meta.url),'utf8');const reconcile=readFileSync(new URL('../src/lib/shared/chat/read-reconciliation.svelte.ts',import.meta.url),'utf8');const room=readFileSync(new URL('../src/lib/features/room/components/RoomChat.svelte',import.meta.url),'utf8');const ui=readFileSync(new URL('../src/lib/features/home/components/NotificationInbox.svelte',import.meta.url),'utf8');
test('G66-A01 first unread is server-authoritative across pages and read races',async()=>{expect(inbox).toContain('envelope.firstUnread');expect(inbox).toContain('requestGeneration');expect(inbox).toContain('Math.max(revision');expect(reconcile).toContain('if (!cursor || committed) return');});
test('G66-A02 around navigation loads exact history without writing read before render',async()=>{expect(ui).toContain('inbox.firstUnread');expect(room).toContain("searchParams.get('around')");const init=room.slice(room.indexOf('async function initializeHistory'),room.indexOf('function latestReadCursor'));expect(init.indexOf('await history.open(roomId, aroundMessageId)')).toBeLessThan(init.indexOf('markLatestRenderedRead()'));expect(init).not.toMatch(/aroundMessageId[\s\S]{0,120}markRoomChatRead/);});
