/** Shared Lucide defaults — sizes are overridden by parent CSS where needed. */
export const iconStroke = 2;

export const iconXs = { size: 12, strokeWidth: iconStroke } as const;
export const iconSm = { size: 15, strokeWidth: iconStroke } as const;
export const iconMd = { size: 18, strokeWidth: iconStroke } as const;
export const iconLg = { size: 20, strokeWidth: iconStroke } as const;