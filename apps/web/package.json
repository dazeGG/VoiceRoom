{
  "name": "@voice-room/web",
  "version": "2.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "node --check static/audio-gate.worklet.js && node --check static/desktop-audio-source.worklet.js && node --check static/rnnoise/rnnoise.worklet.js && svelte-kit sync && tsc --noEmit",
    "preview": "vite preview",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@voice-room/shared": "file:../../packages/shared",
    "livekit-client": "^2.19.2"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.8",
    "@sveltejs/kit": "^2.0.0",
    "@types/node": "^20.19.0",
    "svelte": "^5.0.0",
    "typescript": "^5.6.0",
    "vite": "^8.0.0"
  }
}
