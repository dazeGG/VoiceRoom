'use strict';

const crypto = require('node:crypto');

class NotificationFenceError extends Error { constructor() { super('Notification delivery fence lost'); this.code='NOTIFICATION_FENCE_LOST'; } }
function mapRow(row) { return row ? { eventId:row.event_id,notificationId:row.notification_id,recipientUserId:row.recipient_user_id,revision:Number(row.revision),channel:row.channel,payload:row.payload,status:row.status,attempts:Number(row.attempts),createdAt:row.created_at } : null; }

function createNotificationOutboxRepository({ pool } = {}) {
  if (!pool?.query) throw new TypeError('A PostgreSQL pool is required');
  const db=(client)=>client?.query?client:pool;
  async function enqueue({ notificationId, recipientUserId, revision, payload, channel='web_push', client }={}) { const eventId=crypto.createHash('sha256').update(`${notificationId}:${revision}:${channel}`).digest('hex'); const r=await db(client).query(`INSERT INTO notification_outbox(event_id,notification_id,recipient_user_id,revision,channel,payload) VALUES($1,$2,$3,$4,$5,$6::jsonb) ON CONFLICT(notification_id,revision,channel) DO UPDATE SET payload=EXCLUDED.payload WHERE notification_outbox.status='pending' RETURNING *`,[eventId,notificationId,recipientUserId,revision,channel,JSON.stringify(payload)]); return mapRow(r.rows[0]); }
  async function acquireLease({identity,ownerId,leaseMs}) { const r=await pool.query(`INSERT INTO notification_delivery_leases(identity,owner_id,fencing_token,expires_at,updated_at) VALUES($1,$2,1,current_timestamp+($3*interval '1 millisecond'),current_timestamp) ON CONFLICT(identity) DO UPDATE SET owner_id=EXCLUDED.owner_id,fencing_token=notification_delivery_leases.fencing_token+1,expires_at=EXCLUDED.expires_at,updated_at=current_timestamp WHERE notification_delivery_leases.expires_at<=current_timestamp OR notification_delivery_leases.owner_id=$2 RETURNING fencing_token,expires_at`,[identity,ownerId,leaseMs]); return r.rows[0]?{acquired:true,fencingToken:Number(r.rows[0].fencing_token),expiresAt:r.rows[0].expires_at}:{acquired:false}; }
  async function renewLease({identity,ownerId,fencingToken,leaseMs}) { const r=await pool.query(`UPDATE notification_delivery_leases SET expires_at=current_timestamp+($4*interval '1 millisecond'),heartbeat_at=current_timestamp,updated_at=current_timestamp WHERE identity=$1 AND owner_id=$2 AND fencing_token=$3 AND expires_at>current_timestamp`,[identity,ownerId,fencingToken,leaseMs]); return {renewed:r.rowCount===1}; }
  async function releaseLease({identity,ownerId,fencingToken}) { await pool.query(`UPDATE notification_delivery_leases SET expires_at='-infinity',ready=false,updated_at=current_timestamp WHERE identity=$1 AND owner_id=$2 AND fencing_token=$3`,[identity,ownerId,fencingToken]); }
  async function recordHeartbeat({identity,ownerId=null,fencingToken=0,ready=false}) { await pool.query(`INSERT INTO notification_delivery_leases(identity,owner_id,fencing_token,expires_at,heartbeat_at,ready) VALUES($1,$2,$3,'-infinity',current_timestamp,$4) ON CONFLICT(identity) DO UPDATE SET heartbeat_at=current_timestamp,ready=$4,updated_at=current_timestamp WHERE notification_delivery_leases.fencing_token<=$3`,[identity,ownerId,fencingToken,ready]); }
  async function claimBatch({identity,ownerId,fencingToken,limit=50,staleClaimMs=120000}) { const r=await pool.query(`WITH fence AS (SELECT 1 FROM notification_delivery_leases WHERE identity=$1 AND owner_id=$2 AND fencing_token=$3 AND expires_at>current_timestamp), candidates AS (SELECT event_id FROM notification_outbox,fence WHERE (status='pending' AND available_at<=current_timestamp) OR (status='processing' AND claimed_at<current_timestamp-($5*interval '1 millisecond')) ORDER BY available_at,created_at,event_id FOR UPDATE OF notification_outbox SKIP LOCKED LIMIT $4) UPDATE notification_outbox o SET status='processing',claimed_at=current_timestamp,claimed_fencing_token=$3,attempts=o.attempts+1,updated_at=current_timestamp FROM candidates WHERE o.event_id=candidates.event_id RETURNING o.*`,[identity,ownerId,fencingToken,limit,staleClaimMs]); return r.rows.map(mapRow); }
  async function finish(eventId,lease,status,extra='') { const r=await pool.query(`UPDATE notification_outbox o SET status=$5, ${extra || "delivered_at=CASE WHEN $5='delivered' THEN current_timestamp ELSE delivered_at END"}, updated_at=current_timestamp FROM notification_delivery_leases l WHERE o.event_id=$1 AND o.claimed_fencing_token=$4 AND l.identity=$2 AND l.owner_id=$3 AND l.fencing_token=$4 AND l.expires_at>current_timestamp`,[eventId,lease.identity,lease.ownerId,lease.fencingToken,status]); if(!r.rowCount) throw new NotificationFenceError(); }
  const markDelivered=(id,lease)=>finish(id,lease,'delivered');
  const markSuppressed=(id,lease)=>finish(id,lease,'suppressed',"suppressed_at=current_timestamp");
  async function reschedule(eventId,lease,{delayMs,error,maxAttempts=8}={}) { const r=await pool.query(`UPDATE notification_outbox o SET status=CASE WHEN attempts >= $5 THEN 'dead' ELSE 'pending' END, dead_at=CASE WHEN attempts >= $5 THEN current_timestamp ELSE dead_at END, available_at=current_timestamp+($6*interval '1 millisecond'),last_error=$7,claimed_at=NULL,claimed_fencing_token=NULL,updated_at=current_timestamp FROM notification_delivery_leases l WHERE o.event_id=$1 AND o.claimed_fencing_token=$4 AND l.identity=$2 AND l.owner_id=$3 AND l.fencing_token=$4 AND l.expires_at>current_timestamp`,[eventId,lease.identity,lease.ownerId,lease.fencingToken,maxAttempts,delayMs,String(error?.message||error||'delivery failed').slice(0,2000)]); if(!r.rowCount) throw new NotificationFenceError(); }
  async function loadCurrent(event,{client}={}) { const r=await db(client).query(`SELECT o.*,n.read_at,n.retracted_at,n.reasons,np.private_notifications,u.dnd,coalesce(nrm.level,'mentions') level FROM notification_outbox o JOIN user_notifications n ON n.id=o.notification_id JOIN users u ON u.id=n.recipient_user_id LEFT JOIN notification_preferences np ON np.user_id=n.recipient_user_id LEFT JOIN notification_room_mutes nrm ON nrm.user_id=n.recipient_user_id AND nrm.room_id=n.room_id WHERE o.event_id=$1`,[event.eventId]); return r.rows[0]||null; }
  async function oldestPendingAgeMs() { const r=await pool.query(`SELECT COALESCE(EXTRACT(EPOCH FROM (current_timestamp-MIN(created_at)))*1000,0)::bigint AS age_ms FROM notification_outbox WHERE status IN ('pending','processing')`); return Number(r.rows[0]?.age_ms||0); }
  return { acquireLease,claimBatch,enqueue,loadCurrent,markDelivered,markSuppressed,oldestPendingAgeMs,recordHeartbeat,releaseLease,renewLease,reschedule };
}

module.exports={NotificationFenceError,createNotificationOutboxRepository};
