'use strict';
const assert=require('node:assert/strict');const test=require('node:test');const {Pool}=require('pg');const {normalizeNotificationLevel}=require('@voice-room/shared/notifications');const {createNotificationService}=require('../src/domains/notifications/notification-service');const {createNotificationStore}=require('../src/lib/notification-store');const {createRoomStore}=require('../src/lib/room-store');const {createUserStore}=require('../src/lib/user-store');const {runMigrations}=require('../src/lib/migrate');const {createTestDatabase}=require('./db-harness');
const SILENT={log(){},info(){},warn(){},error(){}};
async function fixture(t){const {cleanup,databaseUrl}=await createTestDatabase(t);await runMigrations({databaseUrl,logger:SILENT});const users=createUserStore({databaseUrl,logger:SILENT});const rooms=createRoomStore({databaseUrl,logger:SILENT});const created=await users.createUser({login:'g61-user',displayName:'G61 User',password:'password123'});assert.equal(created.status,'created');await rooms.createRoomWithQuota({roomId:'g61-room',creatorIp:'127.0.0.1',isStatic:true,ownerId:created.user.id});return {cleanup,databaseUrl,rooms,users,userId:created.user.id,roomId:'g61-room'};}
test('G61-A01 only all mentions none validate',()=>{assert.equal(normalizeNotificationLevel('all'),'all');assert.equal(normalizeNotificationLevel('mentions'),'mentions');assert.equal(normalizeNotificationLevel('none'),'none');assert.equal(normalizeNotificationLevel('loud'),'mentions');});
test('G61-A02 PostgreSQL persists explicit levels across restart and keeps missing rows conservative',async(t)=>{const f=await fixture(t);let store=createNotificationStore({databaseUrl:f.databaseUrl,logger:SILENT});t.after(async()=>{await store.close();await f.rooms.close();await f.users.close();await f.cleanup();});assert.equal(await store.getRoomLevel(f),'mentions');assert.deepEqual(await store.setRoomLevel({...f,level:'all'}),{ok:true,level:'all'});assert.equal(await store.getRoomLevel(f),'all');await store.close();store=createNotificationStore({databaseUrl:f.databaseUrl,logger:SILENT});assert.equal(await store.getRoomLevel(f),'all');for(const level of ['mentions','none']){assert.deepEqual(await store.setRoomLevel({...f,level}),{ok:true,level});assert.equal(await store.getRoomLevel(f),level);}assert.equal(await store.isRoomMuted(f),true);const pool=new Pool({connectionString:f.databaseUrl});await pool.query('DELETE FROM notification_room_mutes WHERE user_id = $1 AND room_id = $2',[f.userId,f.roomId]);assert.equal(await store.getRoomLevel(f),'mentions');await pool.end();});
test('G61-A03 explicit all replaces legacy mute and concurrent updates retain one valid preference',async(t)=>{const f=await fixture(t);const store=createNotificationStore({databaseUrl:f.databaseUrl,logger:SILENT});const pool=new Pool({connectionString:f.databaseUrl});t.after(async()=>{await pool.end();await store.close();await f.rooms.close();await f.users.close();await f.cleanup();});assert.equal((await store.setRoomMute({...f,muted:true})).status,'muted');assert.equal(await store.getRoomLevel(f),'none');assert.equal((await store.setRoomMute({...f,muted:false})).status,'unmuted');assert.equal(await store.getRoomLevel(f),'all');await store.setRoomMute({...f,muted:true});await store.setRoomLevel({...f,level:'all'});assert.equal(await store.getRoomLevel(f),'all');assert.equal(await store.isRoomMuted(f),false);assert.deepEqual((await store.getPreferences(f.userId)).mutedRoomIds,[]);await Promise.all(Array.from({length:24},(_,index)=>store.setRoomLevel({...f,level:['all','mentions','none'][index%3]})));const rows=await pool.query('SELECT level FROM notification_room_mutes WHERE user_id = $1 AND room_id = $2',[f.userId,f.roomId]);assert.equal(rows.rowCount,1);assert.ok(['all','mentions','none'].includes(rows.rows[0].level));await store.setRoomLevel({...f,level:'all'});assert.equal(await store.getRoomLevel(f),'all');});
test('G61-A04 service preserves the explicit store level on GET and PUT paths',async()=>{const calls=[];const notificationStore={async getRoomLevel(value){calls.push(['get',value]);return'all';},async setRoomLevel(value){calls.push(['set',value]);return{ok:true,level:value.level};}};const service=createNotificationService({pool:{query(){}},inbox:{},notificationStore});assert.equal(await service.getRoomLevel({userId:'user',roomId:'room'}),'all');assert.deepEqual(await service.setRoomLevel({userId:'user',roomId:'room',level:'all'}),{ok:true,level:'all'});assert.deepEqual(calls,[['get',{userId:'user',roomId:'room'}],['set',{userId:'user',roomId:'room',level:'all'}]]);});
