'use strict';
const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const test=require('node:test');const {normalizeNotificationLevel}=require('@voice-room/shared/notifications');
test('G61-A01 only all mentions none validate',()=>{assert.equal(normalizeNotificationLevel('all'),'all');assert.equal(normalizeNotificationLevel('mentions'),'mentions');assert.equal(normalizeNotificationLevel('none'),'none');assert.equal(normalizeNotificationLevel('loud'),'mentions');});
test('G61-A02 all removes legacy mute while mentions and none retain conservative rows',()=>{const s=fs.readFileSync(path.resolve(__dirname,'../src/lib/notification-store.js'),'utf8');const start=s.indexOf('async function setRoomLevel');const end=s.indexOf('async function isRoomMuted',start);const f=s.slice(start,end);assert.match(f,/level === 'all'[\s\S]*DELETE FROM notification_room_mutes/);assert.match(f,/else[\s\S]*INSERT INTO notification_room_mutes/);});
