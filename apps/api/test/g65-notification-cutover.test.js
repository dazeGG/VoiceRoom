'use strict';
const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const test=require('node:test');
test('G65-A01 addressed messages enqueue durable intents without direct provider delivery',()=>{const s=fs.readFileSync(path.resolve(__dirname,'../src/server.js'),'utf8');const start=s.indexOf('async function handleRoomChatPost');const end=s.indexOf('\nasync function',start+20);const handler=s.slice(start,end>start?end:undefined);assert.match(handler,/createAddressedForMessage/);const addressed=handler.slice(handler.indexOf('createAddressedForMessage')-300,handler.indexOf('createAddressedForMessage')+500);assert.doesNotMatch(addressed,/queuePush|sendToUser|provider\.deliver/);});
test('G65-A02 notification claims are worker-only and coordinated with message cutover',()=>{const worker=fs.readFileSync(path.resolve(__dirname,'../src/workers/notification-delivery.js'),'utf8');const config=fs.readFileSync(path.resolve(__dirname,'../src/lib/config.js'),'utf8');assert.match(worker,/NOTIFICATION_DELIVERY_CLAIM_ENABLED/);assert.match(worker,/oldest_pending_exceeded/);assert.match(worker,/provider_failure_threshold/);assert.match(config,/MESSAGE_DIRECT_EMIT_ENABLED and MESSAGE_DELIVERY_CLAIM_ENABLED cannot both be enabled/);});
