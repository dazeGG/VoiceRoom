import test from "node:test"; import assert from "node:assert/strict"; import fs from "node:fs"; import crypto from "node:crypto";
const read=p=>fs.readFileSync(p,"utf8"); const json=p=>JSON.parse(read(p));
test("canonical docs and historical pointers are tracked",()=>{const p=read("docs/RELEASE_2.5.0_PLAN.md"),s=read("docs/RELEASE_2.5.0_TEST_SPEC.md"); assert.match(p,/### G01 — canonical unified plan/); assert.match(s,/G01 canonical unified plan/); for(const v of ["2.6.0","2.7.0"]) assert.match(read(`docs/RELEASE_${v}_PLAN.md`),/^# Superseded target plan/)});
test("authority and candidate registries are truthful",()=>{const a=json("docs/releases/2.5.0/evidence/archive-authority.json"); assert.equal(a.decision.startG01,true); assert.equal(a.status,"ARCHIVE_AUTHORITY_GREEN"); const r=json("docs/releases/2.5.0/evidence/bootstrap-attempts.json"); assert.equal(r.state,"G01_PRE_BRANCH"); assert.equal(r.currentAttempt,null); assert.equal(fs.existsSync("docs/releases/2.5.0/evidence/bootstrap-lineage.json"),false)});
test("authority bytes match approved handoff digest",()=>{const h=crypto.createHash("sha256").update(fs.readFileSync("docs/releases/2.5.0/evidence/archive-authority.json")).digest("hex"); assert.equal(h,"553190685993cebd114b4ab13402085f053914fd4c2c26e793b466e0ebe3670f")});
