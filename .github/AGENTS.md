<!-- OMX:AGENTS-INIT:MANAGED -->
<!-- Parent: ../AGENTS.md -->
# .github

This AGENTS.md scopes guidance to `.github`. Parent AGENTS guidance still applies unless this file narrows it for this subtree.

## Bootstrap Guardrails
- This is a lightweight scaffold generated by `omx agents-init`.
- Refresh updates the layout summary below and preserves the manual notes block.
- Keep only directory-specific guidance here; do not duplicate the root orchestration brain.

## Current Layout

### Files
- None

### Subdirectories
- `workflows/`

<!-- OMX:AGENTS-INIT:MANUAL:START -->
## Local Notes
- Pull requests into `develop` and `main` must run policy, check, build, and test gates.
- Pushes to `develop` run verification only. Production deployment is allowed only for a successful push to `main`.
- Keep deploy jobs dependent on every required verification job and keep production deploys serialized.
- Workflow changes must preserve least-privilege permissions and must not print secrets.
- Release automation must follow `../docs/GIT_FLOW.md`: release branch to `main`, version/tag validation, then back-merge to `develop`.
<!-- OMX:AGENTS-INIT:MANUAL:END -->
